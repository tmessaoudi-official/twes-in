# twes-in development and CI environment. Ports and the database password come from ./.env (committed defaults).
name: twes-in

services:
  postgres:
    image: postgres:18.6-trixie
    environment:
      POSTGRES_DB: twes
      POSTGRES_USER: twes
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-twes}
    volumes:
      - postgres-data:/var/lib/postgresql
      - ./infra/postgres/init.sql:/docker-entrypoint-initdb.d/10-init.sql:ro
    ports:
      - "${POSTGRES_PORT:-5433}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U twes -d twes"]
      interval: 3s
      timeout: 3s
      retries: 20

  api:
    build:
      context: .
      dockerfile: infra/api/Dockerfile
    environment:
      APP_ENV: dev
      DATABASE_URL: "postgresql://twes:${POSTGRES_PASSWORD:-twes}@postgres:5432/twes?serverVersion=18&charset=utf8"
      MAILER_DSN: smtp://mailpit:1025
      DEFAULT_URI: "http://localhost:${WEB_PORT:-8090}"
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${API_PORT:-8091}:80"

  web:
    build:
      context: .
      dockerfile: infra/web/Dockerfile
      # The web image generates its TypeScript types from the OpenAPI document the api image exports at build.
      additional_contexts:
        api: service:api
    depends_on:
      api:
        condition: service_healthy
    ports:
      - "${WEB_PORT:-8090}:80"

  gotenberg:
    image: gotenberg/gotenberg:8.36.0

  mailpit:
    image: axllent/mailpit:v1.31.1
    ports:
      - "${MAILPIT_UI_PORT:-8092}:8025"
      - "${MAILPIT_SMTP_PORT:-8093}:1025"

volumes:
  postgres-data:
