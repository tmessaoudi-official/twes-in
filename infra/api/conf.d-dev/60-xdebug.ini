; This file is part of twes-in.
;
; (c) Takieddine MESSAOUDI <takieddine.messaoudi.official@gmail.com>
;
; SPDX-License-Identifier: AGPL-3.0-or-later

; ==============================================================================================================
; XDEBUG — development only. Copied into the `dev` Dockerfile target and PRESENT IN NO OTHER IMAGE.
;
; The extension itself is not configured here as "on". `xdebug.mode` is read from the XDEBUG_MODE environment
; variable, which the dev image defaults to `off`, so the cost of having Xdebug installed is ~zero until somebody
; asks for it. That default is the difference between a debugger a developer uses and one they disable on the
; second day: `xdebug.mode=debug` costs roughly 2-5x on EVERY request even with no IDE attached, because every
; function entry and exit goes through the debug handler.
;
; `make debug-on` sets XDEBUG_MODE=debug,develop and recreates the container; `make debug-off` puts it back.
; ==============================================================================================================

zend_extension=xdebug

; From the environment, so flipping it is a compose variable rather than an edit to a file inside an image.
; `off`      — installed, inert, no measurable cost.
; `develop`  — better var_dump, real stack traces on errors. Cheap.
; `debug`    — the step debugger. This is the expensive one.
; `coverage` — needed by PHPUnit for coverage; NOT on by default because it slows the suite substantially.
xdebug.mode=${XDEBUG_MODE}

; ==============================================================================================================
; HOW THE DEBUGGER FINDS THE IDE. This is the part that is normally got wrong, and the symptom of getting it
; wrong is silence — no error, no connection, a breakpoint that simply never hits.
; ==============================================================================================================

; `trigger`, not `yes`. With `yes`, EVERY request tries to open a debug connection to the IDE, and when no IDE is
; listening each one waits for the connect to fail — which reads as the whole API being slow for no reason. With
; `trigger`, a request is debugged only when it carries `XDEBUG_TRIGGER` (as a cookie, GET/POST parameter, or the
; `XDEBUG_SESSION` cookie every IDE's browser extension sets). So the default state is fast, and debugging is
; opt-in per request — which also means one developer can debug while the stack keeps serving normally.
xdebug.start_with_request=trigger

; ==============================================================================================================
; AND THE TRIGGER MUST CARRY A SECRET. `start_with_request=trigger` with an EMPTY `trigger_value` means ANY value
; of `XDEBUG_TRIGGER` arms a session, which is a real remote-code-execution path rather than a theoretical one:
;
;   1. the developer runs `make debug-on` and leaves the IDE listening on 9003;
;   2. any page they subsequently visit runs
;        fetch('http://localhost:8080/health?XDEBUG_TRIGGER=1', {mode: 'no-cors'})
;      -- no CORS and no CSRF token are needed, because the attacker never reads the response. The SIDE EFFECT is
;      the debug session;
;   3. Xdebug dials out to `host.docker.internal:9003`, and DBGp `eval` is arbitrary PHP inside the container that
;      holds `DATABASE_URL`, with full source read through `property_get`.
;
; `xdebug.log_level=1` below means a SUCCESSFUL connection logs nothing, so there is no trace of it either.
;
; With a value set, Xdebug ignores any trigger whose value does not match. The default below is a placeholder
; rather than a secret; `TWES_XDEBUG_TRIGGER` overrides it, and `make debug-on` prints whatever is in force so the
; developer can configure their browser extension. A shared local default is still a large improvement on "any
; value works", because the attack above requires no knowledge of the target at all.
; ==============================================================================================================
xdebug.trigger_value=${TWES_XDEBUG_TRIGGER}

; `host.docker.internal` resolves to the machine running Docker. On Linux it is NOT automatic — Docker Desktop
; provides it on macOS and Windows, and on Linux it exists only because `compose.override.yaml` declares
; `extra_hosts: host.docker.internal:host-gateway`. Without that line this value silently resolves to nothing and
; the debugger never connects, which is the single most common cause of "breakpoints do not work in Docker".
xdebug.client_host=host.docker.internal

; 9003 is Xdebug 3's default and what every current IDE listens on by default. Xdebug 2 used 9000, which also
; happens to be php-fpm's port — a collision that wasted a great deal of the world's time and is why it moved.
xdebug.client_port=9003

; So the IDE can match an incoming connection to this project rather than asking every time.
xdebug.idekey=twes

; ==============================================================================================================
; PATH MAPPING is configured in the IDE, not here, but the values it needs are these:
;
;   remote (in the container):  /app
;   local  (on the host):       <repository>/api
;
; The dev overlay bind-mounts the host `api/` tree at `/app`, so the mapping is one entry and the paths otherwise
; correspond exactly. This is also why `api/vendor` must exist ON THE HOST (`make install`): without it the IDE
; has no file to open when execution steps into a Symfony or Doctrine frame, and the debugger appears to jump into
; nothing.
; ==============================================================================================================

; Log ONLY real problems. Level 7 is the default and writes a line for every connection attempt, which on a
; trigger-based setup means a noisy log full of successful no-ops. Level 1 keeps errors, which is what is useful
; when a connection is NOT being made.
xdebug.log_level=1
xdebug.log=/app/var/log/xdebug.log

; How much of a variable the IDE is shown when it asks. Larger than the defaults because a truncated invoice
; aggregate is the one thing you are looking at when you set the breakpoint.
xdebug.var_display_max_children=256
xdebug.var_display_max_data=4096
xdebug.var_display_max_depth=6

; ==============================================================================================================
; NO `max_execution_time` HERE, AND ITS REMOVAL ON 2026-08-05 FIXED A SETTING THAT WAS SILENTLY DELETING ANOTHER.
;
; This file used to end with `max_execution_time=0`, justified as *"the step debugger halts the process while the
; IDE thinks, so the request time limit must not apply"*. Two things were wrong with that:
;
;   - THE CONDITION IT NAMES IS FALSE BY DEFAULT. `XDEBUG_MODE` is `off` unless `make debug-on` is run, which is
;     the state a developer is in almost all the time -- so the exemption applied when there was no debugger to
;     exempt, and an accidental infinite loop pinned a worker thread forever instead of dying.
;   - IT KILLED `50-dev.ini`'s SETTING. PHP reads the scan directory in filename order, so `zz-60-` always beat
;     `zz-50-`'s `max_execution_time=120`, and that file's comment described something that never applied. Three
;     separate certification reviewers filed this independently.
;
;     A NOTE ON THE EVIDENCE, because all three reviewers and I first cited a measurement that cannot support the
;     claim. `php -r 'echo ini_get("max_execution_time");'` returns `0` in ANY container, configured or not: the CLI
;     SAPI hardcodes it, and it does so as a master value so even `ini_get_all()['global_value']` reads `0`.
;     [Verified: an ini file saying `max_execution_time=77` still reports global=0 local=0 under `php -r`.] The web
;     SAPI cannot be probed here either, because the Caddyfile executes only the front controller and refuses an
;     arbitrary `.php` -- which is a security property working correctly.
;
;     So the ORDERING is [Verified] (a later-sorting scan-dir file wins: `memory_limit` 333M beat 111M in a direct
;     test) and the resulting WEB value is [Inferred] from it rather than observed. The fix stands on the ordering
;     alone: a file that unconditionally sets 0 and sorts last removes the sibling's limit whatever the SAPI does.
;
; The limit now lives in `50-dev.ini` alone, driven by `TWES_MAX_EXECUTION_TIME`, and `make debug-on` sets it to 0
; for the session -- so it is unbounded exactly when a debugger really is attached and bounded otherwise.
; ==============================================================================================================
