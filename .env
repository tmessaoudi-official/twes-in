# Compose variables (committed development defaults). Host ports chosen outside the ranges other local stacks use.
POSTGRES_PASSWORD=twes
POSTGRES_PORT=5433
API_PORT=8091
WEB_PORT=8090
MAILPIT_UI_PORT=8092
MAILPIT_SMTP_PORT=8093
